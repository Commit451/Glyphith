package com.nt.ledlighttest;

import android.os.Bundle;
import android.util.Log;
import android.widget.SeekBar;
import android.widget.TextView;
import androidx.appcompat.app.AppCompatActivity;
import java.io.BufferedReader;
import java.io.BufferedWriter;
import java.io.FileReader;
import java.io.FileWriter;

public class MainActivity extends AppCompatActivity {
  private static final String LIGHTBELT_BATTERY = "sys/devices/platform/soc/984000.i2c/i2c-0/0-0020/leds/aw210xx_led/round_leds_br";

  private static final String LIGHTBELT_BRIGHTNESS1 = "sys/devices/platform/soc/984000.i2c/i2c-0/0-0020/leds/aw210xx_led/all_white_leds_br";

  private static final String LIGHTBELT_BRIGHTNESS2 = "sys/devices/platform/soc/984000.i2c/i2c-0/0-0020/leds/aw210xx_led/glo_current";

  private static final String LIGHTBELT_FRONT_CAMERA = "sys/devices/platform/soc/984000.i2c/i2c-0/0-0020/leds/aw210xx_led/front_cam_led_br";

  private static final String LIGHTBELT_REAR_CAMERA = "sys/devices/platform/soc/984000.i2c/i2c-0/0-0020/leds/aw210xx_led/rear_cam_led_br";

  private static final String LIGHTBELT_USB = "sys/devices/platform/soc/984000.i2c/i2c-0/0-0020/leds/aw210xx_led/horse_race_leds_br";

  private static final String TAG = "LedLightTest_MainActivity";

  private SeekBar mBrightness1;

  private SeekBar mBrightness2;

  private SeekBar mBrightnessBattery;

  private SeekBar mBrightnessFrontCamera;

  private SeekBar mBrightnessRearCamera;

  private SeekBar mBrightnessUsb;

  private TextView mTextView1;

  private TextView mTextView2;

  private TextView mTextViewBattery;

  private TextView mTextViewFrontCamera;

  private TextView mTextViewRearCamera;

  private TextView mTextViewUsb;

  private String getNodeString(String paramString) {
    String str;
    try {
      BufferedReader bufferedReader = new BufferedReader();
      FileReader fileReader = new FileReader();
      this(paramString);
      this(fileReader);
      paramString = bufferedReader.readLine();
    } catch (Exception exception) {
      Log.i("LedLightTest_MainActivity", "getNodeString: );
      str = "0";
    }
    Log.i("LedLightTest_MainActivity", "getNodeString: value = " + str);
    return str;
  }

  private void initData() {
    String str = getNodeString("sys/devices/platform/soc/984000.i2c/i2c-0/0-0020/leds/aw210xx_led/all_white_leds_br");
    Log.i("LedLightTest_MainActivity", "initData: brightness1 = " + str);
    if (str != null && !str.isEmpty()) {
      this.mBrightness1.setProgress(Integer.parseInt(str));
      this.mTextView1.setText(str);
    } else {
      this.mBrightness1.setProgress(0);
      this.mTextView1.setText(0);
    }
    str = getNodeString("sys/devices/platform/soc/984000.i2c/i2c-0/0-0020/leds/aw210xx_led/glo_current");
    Log.i("LedLightTest_MainActivity", "initData: brightness2 = " + str);
    if (str != null && !str.isEmpty()) {
      this.mBrightness2.setProgress(Integer.parseInt(str));
      this.mTextView2.setText(str);
    } else {
      this.mBrightness2.setProgress(0);
      this.mTextView2.setText(0);
    }
    str = getNodeString("sys/devices/platform/soc/984000.i2c/i2c-0/0-0020/leds/aw210xx_led/front_cam_led_br");
    Log.i("LedLightTest_MainActivity", "initData: brightnessFrontCamera = " + str);
    if (str != null && !str.isEmpty()) {
      this.mBrightnessFrontCamera.setProgress(Integer.parseInt(str));
      this.mTextViewFrontCamera.setText(str);
    } else {
      this.mBrightnessFrontCamera.setProgress(0);
      this.mTextViewFrontCamera.setText(0);
    }
    str = getNodeString("sys/devices/platform/soc/984000.i2c/i2c-0/0-0020/leds/aw210xx_led/rear_cam_led_br");
    Log.i("LedLightTest_MainActivity", "initData: BrightnessRearCamera = " + str);
    if (str != null && !str.isEmpty()) {
      this.mBrightnessRearCamera.setProgress(Integer.parseInt(str));
      this.mTextViewRearCamera.setText(str);
    } else {
      this.mBrightnessRearCamera.setProgress(0);
      this.mTextViewRearCamera.setText(0);
    }
    str = getNodeString("sys/devices/platform/soc/984000.i2c/i2c-0/0-0020/leds/aw210xx_led/horse_race_leds_br");
    Log.i("LedLightTest_MainActivity", "initData: brightnessUsb = " + str);
    if (str != null && !str.isEmpty()) {
      this.mBrightnessUsb.setProgress(Integer.parseInt(str));
      this.mTextViewUsb.setText(str);
    } else {
      this.mBrightnessUsb.setProgress(0);
      this.mTextViewUsb.setText(0);
    }
    str = getNodeString("sys/devices/platform/soc/984000.i2c/i2c-0/0-0020/leds/aw210xx_led/round_leds_br");
    Log.i("LedLightTest_MainActivity", "initData: brightnessBattery = " + str);
    if (str != null && !str.isEmpty()) {
      this.mBrightnessBattery.setProgress(Integer.parseInt(str));
      this.mTextViewBattery.setText(str);
    } else {
      this.mBrightnessBattery.setProgress(0);
      this.mTextViewBattery.setText(0);
    }
  }

  private void initView() {
    this.mBrightness1 = (SeekBar)findViewById(2131230930);
    this.mBrightness2 = (SeekBar)findViewById(2131230931);
    this.mBrightnessFrontCamera = (SeekBar)findViewById(2131230934);
    this.mBrightnessRearCamera = (SeekBar)findViewById(2131230936);
    this.mBrightnessUsb = (SeekBar)findViewById(2131230938);
    this.mBrightnessBattery = (SeekBar)findViewById(2131230932);
    this.mTextView1 = (TextView)findViewById(2131230941);
    this.mTextView2 = (TextView)findViewById(2131230942);
    this.mTextViewFrontCamera = (TextView)findViewById(2131230935);
    this.mTextViewRearCamera = (TextView)findViewById(2131230937);
    this.mTextViewUsb = (TextView)findViewById(2131230939);
    this.mTextViewBattery = (TextView)findViewById(2131230933);
  }

  private void setListener() {
    this.mBrightness1.setOnSeekBarChangeListener(new SeekBar.OnSeekBarChangeListener() {
          public void onProgressChanged(SeekBar param1SeekBar, int param1Int, boolean param1Boolean) {
            MainActivity.this.setNodeString("sys/devices/platform/soc/984000.i2c/i2c-0/0-0020/leds/aw210xx_led/all_white_leds_br", param1Int + "");
            MainActivity.this.mTextView1.setText(param1Int + "");
          }

          public void onStartTrackingTouch(SeekBar param1SeekBar) {}

          public void onStopTrackingTouch(SeekBar param1SeekBar) {}
        });
    this.mBrightness2.setOnSeekBarChangeListener(new SeekBar.OnSeekBarChangeListener() {
          public void onProgressChanged(SeekBar param1SeekBar, int param1Int, boolean param1Boolean) {
            MainActivity.this.setNodeString("sys/devices/platform/soc/984000.i2c/i2c-0/0-0020/leds/aw210xx_led/glo_current", param1Int + "");
            MainActivity.this.mTextView2.setText(param1Int + "");
          }

          public void onStartTrackingTouch(SeekBar param1SeekBar) {}

          public void onStopTrackingTouch(SeekBar param1SeekBar) {}
        });
    this.mBrightnessFrontCamera.setOnSeekBarChangeListener(new SeekBar.OnSeekBarChangeListener() {
          public void onProgressChanged(SeekBar param1SeekBar, int param1Int, boolean param1Boolean) {
            MainActivity.this.setNodeString("sys/devices/platform/soc/984000.i2c/i2c-0/0-0020/leds/aw210xx_led/front_cam_led_br", param1Int + "");
            MainActivity.this.mTextViewFrontCamera.setText(param1Int + "");
          }

          public void onStartTrackingTouch(SeekBar param1SeekBar) {}

          public void onStopTrackingTouch(SeekBar param1SeekBar) {}
        });
    this.mBrightnessRearCamera.setOnSeekBarChangeListener(new SeekBar.OnSeekBarChangeListener() {
          public void onProgressChanged(SeekBar param1SeekBar, int param1Int, boolean param1Boolean) {
            MainActivity.this.setNodeString("sys/devices/platform/soc/984000.i2c/i2c-0/0-0020/leds/aw210xx_led/rear_cam_led_br", param1Int + "");
            MainActivity.this.mTextViewRearCamera.setText(param1Int + "");
          }

          public void onStartTrackingTouch(SeekBar param1SeekBar) {}

          public void onStopTrackingTouch(SeekBar param1SeekBar) {}
        });
    this.mBrightnessUsb.setOnSeekBarChangeListener(new SeekBar.OnSeekBarChangeListener() {
          public void onProgressChanged(SeekBar param1SeekBar, int param1Int, boolean param1Boolean) {
            MainActivity.this.setNodeString("sys/devices/platform/soc/984000.i2c/i2c-0/0-0020/leds/aw210xx_led/horse_race_leds_br", param1Int + "");
            MainActivity.this.mTextViewUsb.setText(param1Int + "");
          }

          public void onStartTrackingTouch(SeekBar param1SeekBar) {}

          public void onStopTrackingTouch(SeekBar param1SeekBar) {}
        });
    this.mBrightnessBattery.setOnSeekBarChangeListener(new SeekBar.OnSeekBarChangeListener() {
          public void onProgressChanged(SeekBar param1SeekBar, int param1Int, boolean param1Boolean) {
            MainActivity.this.setNodeString("sys/devices/platform/soc/984000.i2c/i2c-0/0-0020/leds/aw210xx_led/round_leds_br", param1Int + "");
            MainActivity.this.mTextViewBattery.setText(param1Int + "");
          }

          public void onStartTrackingTouch(SeekBar param1SeekBar) {}

          public void onStopTrackingTouch(SeekBar param1SeekBar) {}
        });
  }

  private boolean setNodeString(String paramString1, String paramString2) {
    try {
      BufferedWriter bufferedWriter = new BufferedWriter();
      FileWriter fileWriter = new FileWriter();
      this(paramString1);
      this(fileWriter);
      bufferedWriter.write(paramString2);
      bufferedWriter.close();
      Log.i("LedLightTest_MainActivity", "setNodeString: );
      return true;
    } catch (Exception exception) {
      Log.i("LedLightTest_MainActivity", "setNodeString: );
      return false;
    }
  }

  protected void onCreate(Bundle paramBundle) {
    super.onCreate(paramBundle);
    setContentView(2131427356);
    initView();
    setListener();
  }

  protected void onResume() {
    super.onResume();
    initData();
  }
}
